spring.application.name=tracker

spring.profiles.active= dev

# Import with specific ordering and fallbacks
spring.config.import=optional:file:.env[.properties],optional:file:.env.dev[.properties],optional:file:.env.prod[.properties]

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect